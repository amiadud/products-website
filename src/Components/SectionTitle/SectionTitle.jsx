import React from 'react';

const SectionTitle = ({heading}) => {
    return (
        <h2 className='text-4xl text-center capitalize  font-medium'>
          {heading}
        </h2>
    );
};

export default SectionTitle;